# Conversão para `SGDB/veloz-pass-react`

- [ ] Analisar estrutura atual (backend express em `SGDB/veloz-pass-react/*` e frontend React em `SGDB/veloz-pass-react/src/*`).
- [ ] Identificar quais arquivos do frontend React ainda não existem (ex.: `pages/LoginPage.jsx`, `pages/RegisterPage.jsx`).
- [ ] Ajustar rotas/exports para apontarem para as páginas/componentes corretos.
- [ ] Separar o que deve ficar no React (`src/**`, `public/**`, `index.html`, assets).
- [ ] Gerar lista de arquivos para deletar com base no que NÃO é mais usado pelo React.

