Função para todas as páginas:
- Fazer que o botão escuro mude todas as páginas em conjunto e deixe salvo em todas elas;
- O cadastro do usuário ficará salvo através de um banco de dados, para todas as páginas em que acessar no Veloz Pass (está parte não é necessário para agora!);

index.html (banco de dados):
- Tornar as informações credenciais salvas em um banco de dados para que o usuário não tenha que cadastrar várias vezes;
- Ter uma forma do usuário não criar contas com mesmo número e e-mail (para evitar perfis clonados);

index2.html (banco de dados):
- As informações das últimas recargas realizadas sejam visualizadas dentro do quadrado "últimas recargas" em uma lista de três linhas (sem barra lateral);
- Pegar o valor da carteira digital e demonstrar no "Valor Disponível", onde será atualizado conforme o usuário coloca valor ou retira;
- Mudar "Seja bem vindo, usuário!" para o "Seja bem vindo, 'nome do usuário cadastrado'" (pelo menos, o nome e sobrenome);

index2.html (CSS e Java Script):
- Criar um modal para acessar os "Termos e Políticas" do site.

index3.html (HTML, CSS, Java Script e Banco de Dados):
- Pegar todas as informações do site e colocar em uma lista abaixo dentro da tabela de histórico geral (O comentário no HTML explica melhor).

index4.html (CSS, Java Script e Banco de Dados):
- Mostrar o protocolo do processo na lista abaixo e puxar este dado para a tabela de histórico geral.

index5.html (HTML e CSS):
- Melhorar o layout da página.

index5.html (Java Script e Banco de Dados):
- Mostrar o protocolo do processo na lista abaixo puxar este dado para a tabela de últimas recargas e histórico geral.

index6.html e index7.html (ainda em processo):
- Elaborar o design do site.



